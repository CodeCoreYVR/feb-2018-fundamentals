// arrays
let shoppingList = ["beer", "milk", "eggs"];
shoppingList[0]; // beer

// objects
const car = {
    // name: value
    make: "Tesla",
    model: "Model 3",
    capacity: 5
};
car.make;
car.model;
car.capacity;

console.log(`I am driving a ${car.make} ${car.model} that can seat ${car.capacity} people.`);

const shoppingList2 = {
    0: "beer",
    1: "milk",
    2: "eggs"
}

shoppingList2[0];
shoppingList2[1];
shoppingList2[2];